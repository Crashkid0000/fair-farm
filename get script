local success, scriptContent = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/Crashkid0000/fair-farm/main/script.lua")
end)

if success then
    local f = loadstring(scriptContent)
    if f then f() end
else
    warn("Failed to fetch script:", scriptContent)
end
